using System.Runtime.InteropServices;
using System.Text;
using System.Text.Json.Serialization;
using BenchmarkDotNet.Attributes;
using Parquet;
using Parquet.Serialization;
using ParquetSharp;
using ParquetSharp.IO;
using ParquetSharp.RowOriented;
using Plank.Reading;
using Plank.RowApi;
using Plank.Schema;
using Plank.Writing;
using NativeBuffer = ParquetSharp.IO.Buffer;
using Encoding = System.Text.Encoding;
using LogicalType = ParquetSharp.LogicalType;
using TimeUnit = ParquetSharp.TimeUnit;
using ParquetDataPageVersion = Plank.Writing.ParquetDataPageVersion;

namespace Plank.Benchmarks;

// Generated by scripts/generate_column_cases.py. Row cases remain unchanged.
[MemoryDiagnoser]
public class RealDoublesPlainColumnPlankBenchmarks
{
    const int RowsPerRowGroup = 1048576;
    RealDoublesPlainRow[] _rows = null!;
    MemoryStream _output = null!;
    byte[] _outputBuffer = null!;
    long _expectedOutputBytes;
    double?[] _c0 = null!;
    double?[] _c1 = null!;
    double?[] _c2 = null!;
    double?[] _c3 = null!;
    double?[] _c4 = null!;
    double?[] _c5 = null!;
    double?[] _c6 = null!;
    double?[] _c7 = null!;
    double?[] _c8 = null!;
    double?[] _c9 = null!;
    DefaultParquetBufferPool _pool = null!;
    Plank.Writing.ParquetWriter _writer = null!;
    MemoryReadSource _source = null!;
    Plank.Reading.Logical.ParquetReader _reader = null!;
    SerializedColumn<double?> _s0 = null!;
    SerializedColumn<double?> _s1 = null!;
    SerializedColumn<double?> _s2 = null!;
    SerializedColumn<double?> _s3 = null!;
    SerializedColumn<double?> _s4 = null!;
    SerializedColumn<double?> _s5 = null!;
    SerializedColumn<double?> _s6 = null!;
    SerializedColumn<double?> _s7 = null!;
    SerializedColumn<double?> _s8 = null!;
    SerializedColumn<double?> _s9 = null!;
    public IEnumerable<int> RowCounts => [BenchmarkData.TaxiRows];
    [ParamsSource(nameof(RowCounts))]
    public int Rows { get; set; }
    [GlobalSetup(Target = nameof(Write))]
    public void GlobalSetupWrite()
    {

        _rows ??= BenchmarkData.LoadTaxiRows<RealDoublesPlainRow>();
        _c0 = _rows.Select(value => value.TripDistance).ToArray();
        _c1 = _rows.Select(value => value.FareAmount).ToArray();
        _c2 = _rows.Select(value => value.Extra).ToArray();
        _c3 = _rows.Select(value => value.MtaTax).ToArray();
        _c4 = _rows.Select(value => value.TipAmount).ToArray();
        _c5 = _rows.Select(value => value.TollsAmount).ToArray();
        _c6 = _rows.Select(value => value.ImprovementSurcharge).ToArray();
        _c7 = _rows.Select(value => value.TotalAmount).ToArray();
        _c8 = _rows.Select(value => value.CongestionSurcharge).ToArray();
        _c9 = _rows.Select(value => value.AirportFee).ToArray();
        _rows = null!;
        _pool = new DefaultParquetBufferPool(ParquetBufferRetentionPolicy.ZeroAllocation);
        using var preparationOutput = new MemoryStream();
        _writer = RealDoublesPlainRow.Schema.CreateWriter(preparationOutput, new ParquetWriterOptions
        { Compression = CompressionKind.None, BufferPool = _pool, DataPageVersion = ParquetDataPageVersion.V2,
           WritePageIndexes = true, WritePageCrc = false });
        _s0 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[0]);
        _s1 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[1]);
        _s2 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[2]);
        _s3 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[3]);
        _s4 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[4]);
        _s5 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[5]);
        _s6 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[6]);
        _s7 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[7]);
        _s8 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[8]);
        _s9 = _writer.CreateSerializedColumn<double?>(RealDoublesPlainRow.Schema.LeafColumns[9]);
        _outputBuffer = new byte[BenchmarkFixtures.GetOutputCapacity("RealDoublesPlainColumn", "Plank", out _expectedOutputBytes)];
    }
    [IterationSetup(Target = nameof(Write))]
    public void SetupWrite()
    {
        _output = BenchmarkFixtures.CreateOutput(_outputBuffer);
        _writer.Reset(_output);
    }
    [Benchmark]
    public void Write()
    {
        for (var start = 0; start < Rows; start += RowsPerRowGroup)
        {
            var count = Math.Min(RowsPerRowGroup, Rows - start);
            _s0.Serialize(_c0.AsSpan(start, count));
            _s1.Serialize(_c1.AsSpan(start, count));
            _s2.Serialize(_c2.AsSpan(start, count));
            _s3.Serialize(_c3.AsSpan(start, count));
            _s4.Serialize(_c4.AsSpan(start, count));
            _s5.Serialize(_c5.AsSpan(start, count));
            _s6.Serialize(_c6.AsSpan(start, count));
            _s7.Serialize(_c7.AsSpan(start, count));
            _s8.Serialize(_c8.AsSpan(start, count));
            _s9.Serialize(_c9.AsSpan(start, count));
            var group = _writer.StartRowGroup();
            group.Write(_s0);
            group.Write(_s1);
            group.Write(_s2);
            group.Write(_s3);
            group.Write(_s4);
            group.Write(_s5);
            group.Write(_s6);
            group.Write(_s7);
            group.Write(_s8);
            group.Write(_s9);
        }
        _writer.CloseFile();
    }
    [IterationCleanup(Target = nameof(Write))]
    public void CleanupWrite()
    {
        BenchmarkFixtures.ValidateOutput(_expectedOutputBytes, BenchmarkFixtures.OutputLength(_output));
        _output?.Dispose();
    }
    [GlobalSetup(Target = nameof(Read))]
    public void GlobalSetupRead()
    {
        _pool = new DefaultParquetBufferPool(ParquetBufferRetentionPolicy.ZeroAllocation);
        _source = new MemoryReadSource(BenchmarkFixtures.LoadReadFile("RealDoublesPlain"));
        _reader = new Plank.Reading.Logical.ParquetReader(new Plank.Reading.Logical.ParquetReaderOptions { BufferPool = _pool });
    }
    [IterationSetup(Target = nameof(Read))]
    public void SetupRead()
    {
        _reader.Reset(_source);
    }
    public double VerifyRead()
    {
        double sum = 0;
        long count = 0;
        foreach (var group in _reader.RowGroups)
        {
            foreach (var buffer in group.Column<double?>(0))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(1))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(2))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(3))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(4))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(5))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(6))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(7))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(8))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(9))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
        }
        if (count != (long)Rows * 10) throw new InvalidDataException($"Expected {(long)Rows * 10} values, got {count}.");
        return sum;
    }
    [Benchmark]
    public long Read()
    {
        long count = 0;
        foreach (var group in _reader.RowGroups)
        {
            foreach (var buffer in group.Column<double?>(0))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(1))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(2))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(3))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(4))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(5))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(6))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(7))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(8))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(9))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        if (count != (long)Rows * 10) throw new InvalidDataException($"Expected {(long)Rows * 10} values, got {count}.");
        return count;
    }
    [GlobalCleanup]
    public void Cleanup()
    {
        _writer?.Dispose();
        _reader?.Dispose();
        _source?.Dispose();
        _pool?.Dispose();
        _output?.Dispose();
    }
}
[MemoryDiagnoser]
public class RealDoublesPlainColumnParquetSharpBenchmarks
{
    const int RowsPerRowGroup = 1048576;
    RealDoublesPlainRow[] _rows = null!;
    MemoryStream _output = null!;
    byte[] _outputBuffer = null!;
    long _expectedOutputBytes;
    double?[] _c0 = null!;
    double?[] _c1 = null!;
    double?[] _c2 = null!;
    double?[] _c3 = null!;
    double?[] _c4 = null!;
    double?[] _c5 = null!;
    double?[] _c6 = null!;
    double?[] _c7 = null!;
    double?[] _c8 = null!;
    double?[] _c9 = null!;
    ParquetSharp.Column[] _schema = null!;
    WriterProperties _properties = null!;
    ManagedOutputStream _managedOutput = null!;
    ParquetFileWriter _writer = null!;
    ParquetFileReader _reader = null!;
    GCHandle _pinned;
    NativeBuffer _buffer = null!;
    BufferReader _source = null!;
    double?[] _read0 = new double?[65536];
    double?[] _read1 = new double?[65536];
    double?[] _read2 = new double?[65536];
    double?[] _read3 = new double?[65536];
    double?[] _read4 = new double?[65536];
    double?[] _read5 = new double?[65536];
    double?[] _read6 = new double?[65536];
    double?[] _read7 = new double?[65536];
    double?[] _read8 = new double?[65536];
    double?[] _read9 = new double?[65536];
    public IEnumerable<int> RowCounts => [BenchmarkData.TaxiRows];
    [ParamsSource(nameof(RowCounts))]
    public int Rows { get; set; }
    [GlobalSetup(Target = nameof(Write))]
    public void GlobalSetupWrite()
    {

        _rows ??= BenchmarkData.LoadTaxiRows<RealDoublesPlainRow>();
        _c0 = _rows.Select(value => value.TripDistance).ToArray();
        _c1 = _rows.Select(value => value.FareAmount).ToArray();
        _c2 = _rows.Select(value => value.Extra).ToArray();
        _c3 = _rows.Select(value => value.MtaTax).ToArray();
        _c4 = _rows.Select(value => value.TipAmount).ToArray();
        _c5 = _rows.Select(value => value.TollsAmount).ToArray();
        _c6 = _rows.Select(value => value.ImprovementSurcharge).ToArray();
        _c7 = _rows.Select(value => value.TotalAmount).ToArray();
        _c8 = _rows.Select(value => value.CongestionSurcharge).ToArray();
        _c9 = _rows.Select(value => value.AirportFee).ToArray();
        _rows = null!;
        _schema =
        [
            new ParquetSharp.Column<double?>("trip_distance"),
            new ParquetSharp.Column<double?>("fare_amount"),
            new ParquetSharp.Column<double?>("extra"),
            new ParquetSharp.Column<double?>("mta_tax"),
            new ParquetSharp.Column<double?>("tip_amount"),
            new ParquetSharp.Column<double?>("tolls_amount"),
            new ParquetSharp.Column<double?>("improvement_surcharge"),
            new ParquetSharp.Column<double?>("total_amount"),
            new ParquetSharp.Column<double?>("congestion_surcharge"),
            new ParquetSharp.Column<double?>("Airport_fee"),
        ];
        using var builder = new WriterPropertiesBuilder();
        builder
            .Compression(Compression.Uncompressed)
            .EnableStatistics()
            .EnableWritePageIndex()
            .DataPageVersion(ParquetSharp.ParquetDataPageVersion.V2);
        _properties = builder.DisableDictionary().Encoding(ParquetSharp.Encoding.Plain).Build();


        _outputBuffer = new byte[BenchmarkFixtures.GetOutputCapacity("RealDoublesPlainColumn", "ParquetSharp", out _expectedOutputBytes)];
    }
    [IterationSetup(Target = nameof(Write))]
    public void SetupWrite()
    {
        _output = BenchmarkFixtures.CreateOutput(_outputBuffer);
        _managedOutput = new ManagedOutputStream(_output, leaveOpen: true);
        _writer = new ParquetFileWriter(_managedOutput, _schema, _properties);
    }
    [Benchmark]
    public void Write()
    {
        for (var start = 0; start < Rows; start += RowsPerRowGroup)
        {
            var count = Math.Min(RowsPerRowGroup, Rows - start);
            using var group = _writer.AppendRowGroup();
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c0.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c1.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c2.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c3.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c4.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c5.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c6.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c7.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c8.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<double?>())
                column.WriteBatch(_c9.AsSpan(start, count));
        }
        _writer.Close();
    }
    [IterationCleanup(Target = nameof(Write))]
    public void CleanupWrite()
    {
        BenchmarkFixtures.ValidateOutput(_expectedOutputBytes, BenchmarkFixtures.OutputLength(_output));
        _writer?.Dispose();
        _managedOutput?.Dispose();
        _output?.Dispose();
    }
    [GlobalSetup(Target = nameof(Read))]
    public void GlobalSetupRead()
    {

        var file = BenchmarkFixtures.LoadReadFile("RealDoublesPlain");
        _pinned = GCHandle.Alloc(file, GCHandleType.Pinned);
        _buffer = new NativeBuffer(_pinned.AddrOfPinnedObject(), file.LongLength);
        _source = new BufferReader(_buffer);

    }
    [IterationSetup(Target = nameof(Read))]
    public void SetupRead()
    {
        _reader?.Dispose();
        _reader = new ParquetFileReader(_source);
    }
    public double VerifyRead()
    {
        double sum = 0;
        long count = 0;
        for (var g = 0; g < _reader.FileMetaData.NumRowGroups; g++)
        {
            using var group = _reader.RowGroup(g);
            using (var column = group.Column(0).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read0);
                    sum = ColumnChecksum.Accumulate(_read0.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(1).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read1);
                    sum = ColumnChecksum.Accumulate(_read1.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(2).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read2);
                    sum = ColumnChecksum.Accumulate(_read2.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(3).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read3);
                    sum = ColumnChecksum.Accumulate(_read3.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(4).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read4);
                    sum = ColumnChecksum.Accumulate(_read4.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(5).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read5);
                    sum = ColumnChecksum.Accumulate(_read5.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(6).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read6);
                    sum = ColumnChecksum.Accumulate(_read6.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(7).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read7);
                    sum = ColumnChecksum.Accumulate(_read7.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(8).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read8);
                    sum = ColumnChecksum.Accumulate(_read8.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(9).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read9);
                    sum = ColumnChecksum.Accumulate(_read9.AsSpan(0, length), sum);
                    count += length;
                }
            }
        }
        if (count != (long)Rows * 10) throw new InvalidDataException($"Expected {(long)Rows * 10} values, got {count}.");
        return sum;
    }
    [Benchmark]
    public long Read()
    {
        long count = 0;
        for (var g = 0; g < _reader.FileMetaData.NumRowGroups; g++)
        {
            using var group = _reader.RowGroup(g);
            using (var column = group.Column(0).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read0);
                    ReadConsumption.Consume(_read0.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(1).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read1);
                    ReadConsumption.Consume(_read1.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(2).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read2);
                    ReadConsumption.Consume(_read2.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(3).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read3);
                    ReadConsumption.Consume(_read3.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(4).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read4);
                    ReadConsumption.Consume(_read4.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(5).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read5);
                    ReadConsumption.Consume(_read5.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(6).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read6);
                    ReadConsumption.Consume(_read6.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(7).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read7);
                    ReadConsumption.Consume(_read7.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(8).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read8);
                    ReadConsumption.Consume(_read8.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(9).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read9);
                    ReadConsumption.Consume(_read9.AsSpan(0, length));
                    count += length;
                }
            }
        }
        if (count != (long)Rows * 10) throw new InvalidDataException($"Expected {(long)Rows * 10} values, got {count}.");
        return count;
    }
    [GlobalCleanup]
    public void Cleanup()
    {
        _reader?.Dispose();
        _source?.Dispose();
        _buffer?.Dispose();
        if (_pinned.IsAllocated) _pinned.Free();
        _properties?.Dispose();
        _output?.Dispose();
    }
}
[MemoryDiagnoser]
public class RealDoublesPlainColumnParquetNetBenchmarks
{
    const int RowsPerRowGroup = 1048576;
    RealDoublesPlainRow[] _rows = null!;
    MemoryStream _output = null!;
    byte[] _outputBuffer = null!;
    long _expectedOutputBytes;
    double?[] _c0 = null!;
    double?[] _c1 = null!;
    double?[] _c2 = null!;
    double?[] _c3 = null!;
    double?[] _c4 = null!;
    double?[] _c5 = null!;
    double?[] _c6 = null!;
    double?[] _c7 = null!;
    double?[] _c8 = null!;
    double?[] _c9 = null!;
    ParquetOptions _options = null!;
    Parquet.Schema.ParquetSchema _schema = null!;
    byte[] _file = null!;
    MemoryStream _stream = null!;
    public IEnumerable<int> RowCounts => [BenchmarkData.TaxiRows];
    [ParamsSource(nameof(RowCounts))]
    public int Rows { get; set; }
    [GlobalSetup(Target = nameof(Write))]
    public void GlobalSetupWrite()
    {

        _rows ??= BenchmarkData.LoadTaxiRows<RealDoublesPlainRow>();
        _c0 = _rows.Select(value => value.TripDistance).ToArray();
        _c1 = _rows.Select(value => value.FareAmount).ToArray();
        _c2 = _rows.Select(value => value.Extra).ToArray();
        _c3 = _rows.Select(value => value.MtaTax).ToArray();
        _c4 = _rows.Select(value => value.TipAmount).ToArray();
        _c5 = _rows.Select(value => value.TollsAmount).ToArray();
        _c6 = _rows.Select(value => value.ImprovementSurcharge).ToArray();
        _c7 = _rows.Select(value => value.TotalAmount).ToArray();
        _c8 = _rows.Select(value => value.CongestionSurcharge).ToArray();
        _c9 = _rows.Select(value => value.AirportFee).ToArray();
        _rows = null!;
        _options = new ParquetOptions
        {
            CompressionMethod = CompressionMethod.None,
            DictionaryEncodingThreshold = 0,
            RowGroupSize = 1048576
        };
        _options.ColumnEncodingHints["trip_distance"] = EncodingHint.Default;
        _options.ColumnEncodingHints["fare_amount"] = EncodingHint.Default;
        _options.ColumnEncodingHints["extra"] = EncodingHint.Default;
        _options.ColumnEncodingHints["mta_tax"] = EncodingHint.Default;
        _options.ColumnEncodingHints["tip_amount"] = EncodingHint.Default;
        _options.ColumnEncodingHints["tolls_amount"] = EncodingHint.Default;
        _options.ColumnEncodingHints["improvement_surcharge"] = EncodingHint.Default;
        _options.ColumnEncodingHints["total_amount"] = EncodingHint.Default;
        _options.ColumnEncodingHints["congestion_surcharge"] = EncodingHint.Default;
        _options.ColumnEncodingHints["Airport_fee"] = EncodingHint.Default;

        _schema = new Parquet.Schema.ParquetSchema(new Parquet.Schema.DataField<double?>("trip_distance", true), new Parquet.Schema.DataField<double?>("fare_amount", true), new Parquet.Schema.DataField<double?>("extra", true), new Parquet.Schema.DataField<double?>("mta_tax", true), new Parquet.Schema.DataField<double?>("tip_amount", true), new Parquet.Schema.DataField<double?>("tolls_amount", true), new Parquet.Schema.DataField<double?>("improvement_surcharge", true), new Parquet.Schema.DataField<double?>("total_amount", true), new Parquet.Schema.DataField<double?>("congestion_surcharge", true), new Parquet.Schema.DataField<double?>("Airport_fee", true));

        _outputBuffer = new byte[BenchmarkFixtures.GetOutputCapacity("RealDoublesPlainColumn", "Parquet.Net", out _expectedOutputBytes)];
    }
    [IterationSetup(Target = nameof(Write))]
    public void SetupWrite()
    {
        _output = BenchmarkFixtures.CreateOutput(_outputBuffer);

    }
    [Benchmark]
    public async Task Write()
    {
        await using var writer = await Parquet.ParquetWriter.CreateAsync(_schema, _output, _options);
        var fields = _schema.GetDataFields();
        for (var start = 0; start < Rows; start += RowsPerRowGroup)
        {
            var count = Math.Min(RowsPerRowGroup, Rows - start);
            using var group = writer.CreateRowGroup();
            await group.WriteAsync<double>(fields[0], _c0.AsMemory(start, count));
            await group.WriteAsync<double>(fields[1], _c1.AsMemory(start, count));
            await group.WriteAsync<double>(fields[2], _c2.AsMemory(start, count));
            await group.WriteAsync<double>(fields[3], _c3.AsMemory(start, count));
            await group.WriteAsync<double>(fields[4], _c4.AsMemory(start, count));
            await group.WriteAsync<double>(fields[5], _c5.AsMemory(start, count));
            await group.WriteAsync<double>(fields[6], _c6.AsMemory(start, count));
            await group.WriteAsync<double>(fields[7], _c7.AsMemory(start, count));
            await group.WriteAsync<double>(fields[8], _c8.AsMemory(start, count));
            await group.WriteAsync<double>(fields[9], _c9.AsMemory(start, count));
        }
    }
    [IterationCleanup(Target = nameof(Write))]
    public void CleanupWrite()
    {
        BenchmarkFixtures.ValidateOutput(_expectedOutputBytes, BenchmarkFixtures.OutputLength(_output));
        _output?.Dispose();
    }
    [GlobalSetup(Target = nameof(Read))]
    public void GlobalSetupRead()
    {
        _file = BenchmarkFixtures.LoadReadFile("RealDoublesPlain");
    }
    [IterationSetup(Target = nameof(Read))]
    public void SetupRead()
    {
        _stream?.Dispose();
        _stream = new MemoryStream(_file, writable: false);
    }
    public async Task<double> VerifyRead()
    {
        double sum = 0;
        long count = 0;
        await using var reader = await Parquet.ParquetReader.CreateAsync(_stream);
        var fields = reader.Schema.GetDataFields();
        for (var g = 0; g < reader.RowGroupCount; g++)
        {
            using var group = reader.OpenRowGroupReader(g);
            var column0 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[0], column0.AsMemory());
            sum = ColumnChecksum.Accumulate(column0.AsSpan(), sum);
            count += column0.Length;
            var column1 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[1], column1.AsMemory());
            sum = ColumnChecksum.Accumulate(column1.AsSpan(), sum);
            count += column1.Length;
            var column2 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[2], column2.AsMemory());
            sum = ColumnChecksum.Accumulate(column2.AsSpan(), sum);
            count += column2.Length;
            var column3 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[3], column3.AsMemory());
            sum = ColumnChecksum.Accumulate(column3.AsSpan(), sum);
            count += column3.Length;
            var column4 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[4], column4.AsMemory());
            sum = ColumnChecksum.Accumulate(column4.AsSpan(), sum);
            count += column4.Length;
            var column5 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[5], column5.AsMemory());
            sum = ColumnChecksum.Accumulate(column5.AsSpan(), sum);
            count += column5.Length;
            var column6 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[6], column6.AsMemory());
            sum = ColumnChecksum.Accumulate(column6.AsSpan(), sum);
            count += column6.Length;
            var column7 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[7], column7.AsMemory());
            sum = ColumnChecksum.Accumulate(column7.AsSpan(), sum);
            count += column7.Length;
            var column8 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[8], column8.AsMemory());
            sum = ColumnChecksum.Accumulate(column8.AsSpan(), sum);
            count += column8.Length;
            var column9 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[9], column9.AsMemory());
            sum = ColumnChecksum.Accumulate(column9.AsSpan(), sum);
            count += column9.Length;
        }
        if (count != (long)Rows * 10) throw new InvalidDataException($"Expected {(long)Rows * 10} values, got {count}.");
        return sum;
    }
    [Benchmark]
    public async Task<long> Read()
    {
        long count = 0;
        await using var reader = await Parquet.ParquetReader.CreateAsync(_stream);
        var fields = reader.Schema.GetDataFields();
        for (var g = 0; g < reader.RowGroupCount; g++)
        {
            using var group = reader.OpenRowGroupReader(g);
            var column0 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[0], column0.AsMemory());
            ReadConsumption.Consume(column0.AsSpan());
            count += column0.Length;
            var column1 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[1], column1.AsMemory());
            ReadConsumption.Consume(column1.AsSpan());
            count += column1.Length;
            var column2 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[2], column2.AsMemory());
            ReadConsumption.Consume(column2.AsSpan());
            count += column2.Length;
            var column3 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[3], column3.AsMemory());
            ReadConsumption.Consume(column3.AsSpan());
            count += column3.Length;
            var column4 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[4], column4.AsMemory());
            ReadConsumption.Consume(column4.AsSpan());
            count += column4.Length;
            var column5 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[5], column5.AsMemory());
            ReadConsumption.Consume(column5.AsSpan());
            count += column5.Length;
            var column6 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[6], column6.AsMemory());
            ReadConsumption.Consume(column6.AsSpan());
            count += column6.Length;
            var column7 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[7], column7.AsMemory());
            ReadConsumption.Consume(column7.AsSpan());
            count += column7.Length;
            var column8 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[8], column8.AsMemory());
            ReadConsumption.Consume(column8.AsSpan());
            count += column8.Length;
            var column9 = new double?[checked((int)group.RowCount)];
            await group.ReadAsync<double>(fields[9], column9.AsMemory());
            ReadConsumption.Consume(column9.AsSpan());
            count += column9.Length;
        }
        if (count != (long)Rows * 10) throw new InvalidDataException($"Expected {(long)Rows * 10} values, got {count}.");
        return count;
    }
    [GlobalCleanup]
    public void Cleanup()
    {
        _stream?.Dispose();
        _output?.Dispose();
    }
}
