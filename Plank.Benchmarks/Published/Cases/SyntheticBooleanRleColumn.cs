using System.Runtime.InteropServices;
using System.Text;
using System.Text.Json.Serialization;
using BenchmarkDotNet.Attributes;
using Parquet;
using Parquet.Serialization;
using ParquetSharp;
using ParquetSharp.IO;
using ParquetSharp.RowOriented;
using Plank.Reading;
using Plank.RowApi;
using Plank.Schema;
using Plank.Writing;
using NativeBuffer = ParquetSharp.IO.Buffer;
using Encoding = System.Text.Encoding;
using LogicalType = ParquetSharp.LogicalType;
using TimeUnit = ParquetSharp.TimeUnit;
using ParquetDataPageVersion = Plank.Writing.ParquetDataPageVersion;

namespace Plank.Benchmarks;

// Generated by scripts/generate_column_cases.py. Row cases remain unchanged.
[MemoryDiagnoser]
public class SyntheticBooleanRleColumnPlankBenchmarks
{
    const int RowsPerRowGroup = 45455;
    SyntheticBooleanRleRow[] _rows = null!;
    MemoryStream _output = null!;
    byte[] _outputBuffer = null!;
    long _expectedOutputBytes;
    bool[] _c0 = null!;
    bool[] _c1 = null!;
    bool[] _c2 = null!;
    bool[] _c3 = null!;
    bool[] _c4 = null!;
    bool[] _c5 = null!;
    bool[] _c6 = null!;
    bool[] _c7 = null!;
    bool[] _c8 = null!;
    bool[] _c9 = null!;
    bool[] _c10 = null!;
    bool[] _c11 = null!;
    bool[] _c12 = null!;
    bool[] _c13 = null!;
    bool[] _c14 = null!;
    bool[] _c15 = null!;
    bool[] _c16 = null!;
    bool[] _c17 = null!;
    bool[] _c18 = null!;
    bool[] _c19 = null!;
    bool[] _c20 = null!;
    bool[] _c21 = null!;
    DefaultParquetBufferPool _pool = null!;
    Plank.Writing.ParquetWriter _writer = null!;
    MemoryReadSource _source = null!;
    Plank.Reading.Logical.ParquetReader _reader = null!;
    SerializedColumn<bool> _s0 = null!;
    SerializedColumn<bool> _s1 = null!;
    SerializedColumn<bool> _s2 = null!;
    SerializedColumn<bool> _s3 = null!;
    SerializedColumn<bool> _s4 = null!;
    SerializedColumn<bool> _s5 = null!;
    SerializedColumn<bool> _s6 = null!;
    SerializedColumn<bool> _s7 = null!;
    SerializedColumn<bool> _s8 = null!;
    SerializedColumn<bool> _s9 = null!;
    SerializedColumn<bool> _s10 = null!;
    SerializedColumn<bool> _s11 = null!;
    SerializedColumn<bool> _s12 = null!;
    SerializedColumn<bool> _s13 = null!;
    SerializedColumn<bool> _s14 = null!;
    SerializedColumn<bool> _s15 = null!;
    SerializedColumn<bool> _s16 = null!;
    SerializedColumn<bool> _s17 = null!;
    SerializedColumn<bool> _s18 = null!;
    SerializedColumn<bool> _s19 = null!;
    SerializedColumn<bool> _s20 = null!;
    SerializedColumn<bool> _s21 = null!;
    public IEnumerable<int> RowCounts => [BenchmarkData.SyntheticRows];
    [ParamsSource(nameof(RowCounts))]
    public int Rows { get; set; }
    [GlobalSetup(Target = nameof(Write))]
    public void GlobalSetupWrite()
    {

        _rows ??= SyntheticBooleanRleRow.CreateRows(Rows);
        _c0 = _rows.Select(value => value.Value0).ToArray();
        _c1 = _rows.Select(value => value.Value1).ToArray();
        _c2 = _rows.Select(value => value.Value2).ToArray();
        _c3 = _rows.Select(value => value.Value3).ToArray();
        _c4 = _rows.Select(value => value.Value4).ToArray();
        _c5 = _rows.Select(value => value.Value5).ToArray();
        _c6 = _rows.Select(value => value.Value6).ToArray();
        _c7 = _rows.Select(value => value.Value7).ToArray();
        _c8 = _rows.Select(value => value.Value8).ToArray();
        _c9 = _rows.Select(value => value.Value9).ToArray();
        _c10 = _rows.Select(value => value.Value10).ToArray();
        _c11 = _rows.Select(value => value.Value11).ToArray();
        _c12 = _rows.Select(value => value.Value12).ToArray();
        _c13 = _rows.Select(value => value.Value13).ToArray();
        _c14 = _rows.Select(value => value.Value14).ToArray();
        _c15 = _rows.Select(value => value.Value15).ToArray();
        _c16 = _rows.Select(value => value.Value16).ToArray();
        _c17 = _rows.Select(value => value.Value17).ToArray();
        _c18 = _rows.Select(value => value.Value18).ToArray();
        _c19 = _rows.Select(value => value.Value19).ToArray();
        _c20 = _rows.Select(value => value.Value20).ToArray();
        _c21 = _rows.Select(value => value.Value21).ToArray();
        _rows = null!;
        _pool = new DefaultParquetBufferPool(ParquetBufferRetentionPolicy.ZeroAllocation);
        using var preparationOutput = new MemoryStream();
        _writer = SyntheticBooleanRleRow.Schema.CreateWriter(preparationOutput, new ParquetWriterOptions
        { Compression = CompressionKind.None, BufferPool = _pool, DataPageVersion = ParquetDataPageVersion.V2,
           WritePageIndexes = true, WritePageCrc = false });
        _s0 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[0]);
        _s1 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[1]);
        _s2 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[2]);
        _s3 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[3]);
        _s4 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[4]);
        _s5 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[5]);
        _s6 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[6]);
        _s7 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[7]);
        _s8 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[8]);
        _s9 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[9]);
        _s10 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[10]);
        _s11 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[11]);
        _s12 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[12]);
        _s13 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[13]);
        _s14 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[14]);
        _s15 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[15]);
        _s16 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[16]);
        _s17 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[17]);
        _s18 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[18]);
        _s19 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[19]);
        _s20 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[20]);
        _s21 = _writer.CreateSerializedColumn<bool>(SyntheticBooleanRleRow.Schema.LeafColumns[21]);
        _outputBuffer = new byte[BenchmarkFixtures.GetOutputCapacity("SyntheticBooleanRleColumn", "Plank", out _expectedOutputBytes)];
    }
    [IterationSetup(Target = nameof(Write))]
    public void SetupWrite()
    {
        _output = BenchmarkFixtures.CreateOutput(_outputBuffer);
        _writer.Reset(_output);
    }
    [Benchmark]
    public void Write()
    {
        for (var start = 0; start < Rows; start += RowsPerRowGroup)
        {
            var count = Math.Min(RowsPerRowGroup, Rows - start);
            _s0.Serialize(_c0.AsSpan(start, count));
            _s1.Serialize(_c1.AsSpan(start, count));
            _s2.Serialize(_c2.AsSpan(start, count));
            _s3.Serialize(_c3.AsSpan(start, count));
            _s4.Serialize(_c4.AsSpan(start, count));
            _s5.Serialize(_c5.AsSpan(start, count));
            _s6.Serialize(_c6.AsSpan(start, count));
            _s7.Serialize(_c7.AsSpan(start, count));
            _s8.Serialize(_c8.AsSpan(start, count));
            _s9.Serialize(_c9.AsSpan(start, count));
            _s10.Serialize(_c10.AsSpan(start, count));
            _s11.Serialize(_c11.AsSpan(start, count));
            _s12.Serialize(_c12.AsSpan(start, count));
            _s13.Serialize(_c13.AsSpan(start, count));
            _s14.Serialize(_c14.AsSpan(start, count));
            _s15.Serialize(_c15.AsSpan(start, count));
            _s16.Serialize(_c16.AsSpan(start, count));
            _s17.Serialize(_c17.AsSpan(start, count));
            _s18.Serialize(_c18.AsSpan(start, count));
            _s19.Serialize(_c19.AsSpan(start, count));
            _s20.Serialize(_c20.AsSpan(start, count));
            _s21.Serialize(_c21.AsSpan(start, count));
            var group = _writer.StartRowGroup();
            group.Write(_s0);
            group.Write(_s1);
            group.Write(_s2);
            group.Write(_s3);
            group.Write(_s4);
            group.Write(_s5);
            group.Write(_s6);
            group.Write(_s7);
            group.Write(_s8);
            group.Write(_s9);
            group.Write(_s10);
            group.Write(_s11);
            group.Write(_s12);
            group.Write(_s13);
            group.Write(_s14);
            group.Write(_s15);
            group.Write(_s16);
            group.Write(_s17);
            group.Write(_s18);
            group.Write(_s19);
            group.Write(_s20);
            group.Write(_s21);
        }
        _writer.CloseFile();
    }
    [IterationCleanup(Target = nameof(Write))]
    public void CleanupWrite()
    {
        BenchmarkFixtures.ValidateOutput(_expectedOutputBytes, BenchmarkFixtures.OutputLength(_output));
        _output?.Dispose();
    }
    [GlobalSetup(Target = nameof(Read))]
    public void GlobalSetupRead()
    {
        _pool = new DefaultParquetBufferPool(ParquetBufferRetentionPolicy.ZeroAllocation);
        _source = new MemoryReadSource(BenchmarkFixtures.LoadReadFile("SyntheticBooleanRle"));
        _reader = new Plank.Reading.Logical.ParquetReader(new Plank.Reading.Logical.ParquetReaderOptions { BufferPool = _pool });
    }
    [IterationSetup(Target = nameof(Read))]
    public void SetupRead()
    {
        _reader.Reset(_source);
    }
    public ulong VerifyRead()
    {
        ulong sum = 0;
        long count = 0;
        foreach (var group in _reader.RowGroups)
        {
            foreach (var buffer in group.Column<bool>(0))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(1))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(2))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(3))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(4))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(5))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(6))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(7))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(8))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(9))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(10))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(11))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(12))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(13))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(14))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(15))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(16))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(17))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(18))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(19))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(20))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(21))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
        }
        if (count != (long)Rows * 22) throw new InvalidDataException($"Expected {(long)Rows * 22} values, got {count}.");
        return sum;
    }
    [Benchmark]
    public long Read()
    {
        long count = 0;
        foreach (var group in _reader.RowGroups)
        {
            foreach (var buffer in group.Column<bool>(0))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(1))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(2))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(3))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(4))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(5))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(6))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(7))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(8))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(9))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(10))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(11))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(12))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(13))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(14))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(15))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(16))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(17))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(18))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(19))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(20))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<bool>(21))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        if (count != (long)Rows * 22) throw new InvalidDataException($"Expected {(long)Rows * 22} values, got {count}.");
        return count;
    }
    [GlobalCleanup]
    public void Cleanup()
    {
        _writer?.Dispose();
        _reader?.Dispose();
        _source?.Dispose();
        _pool?.Dispose();
        _output?.Dispose();
    }
}
[MemoryDiagnoser]
public class SyntheticBooleanRleColumnParquetSharpBenchmarks
{
    const int RowsPerRowGroup = 45455;
    SyntheticBooleanRleRow[] _rows = null!;
    MemoryStream _output = null!;
    byte[] _outputBuffer = null!;
    long _expectedOutputBytes;
    bool[] _c0 = null!;
    bool[] _c1 = null!;
    bool[] _c2 = null!;
    bool[] _c3 = null!;
    bool[] _c4 = null!;
    bool[] _c5 = null!;
    bool[] _c6 = null!;
    bool[] _c7 = null!;
    bool[] _c8 = null!;
    bool[] _c9 = null!;
    bool[] _c10 = null!;
    bool[] _c11 = null!;
    bool[] _c12 = null!;
    bool[] _c13 = null!;
    bool[] _c14 = null!;
    bool[] _c15 = null!;
    bool[] _c16 = null!;
    bool[] _c17 = null!;
    bool[] _c18 = null!;
    bool[] _c19 = null!;
    bool[] _c20 = null!;
    bool[] _c21 = null!;
    ParquetSharp.Column[] _schema = null!;
    WriterProperties _properties = null!;
    ManagedOutputStream _managedOutput = null!;
    ParquetFileWriter _writer = null!;
    ParquetFileReader _reader = null!;
    GCHandle _pinned;
    NativeBuffer _buffer = null!;
    BufferReader _source = null!;
    bool[] _read0 = new bool[65536];
    bool[] _read1 = new bool[65536];
    bool[] _read2 = new bool[65536];
    bool[] _read3 = new bool[65536];
    bool[] _read4 = new bool[65536];
    bool[] _read5 = new bool[65536];
    bool[] _read6 = new bool[65536];
    bool[] _read7 = new bool[65536];
    bool[] _read8 = new bool[65536];
    bool[] _read9 = new bool[65536];
    bool[] _read10 = new bool[65536];
    bool[] _read11 = new bool[65536];
    bool[] _read12 = new bool[65536];
    bool[] _read13 = new bool[65536];
    bool[] _read14 = new bool[65536];
    bool[] _read15 = new bool[65536];
    bool[] _read16 = new bool[65536];
    bool[] _read17 = new bool[65536];
    bool[] _read18 = new bool[65536];
    bool[] _read19 = new bool[65536];
    bool[] _read20 = new bool[65536];
    bool[] _read21 = new bool[65536];
    public IEnumerable<int> RowCounts => [BenchmarkData.SyntheticRows];
    [ParamsSource(nameof(RowCounts))]
    public int Rows { get; set; }
    [GlobalSetup(Target = nameof(Write))]
    public void GlobalSetupWrite()
    {

        _rows ??= SyntheticBooleanRleRow.CreateRows(Rows);
        _c0 = _rows.Select(value => value.Value0).ToArray();
        _c1 = _rows.Select(value => value.Value1).ToArray();
        _c2 = _rows.Select(value => value.Value2).ToArray();
        _c3 = _rows.Select(value => value.Value3).ToArray();
        _c4 = _rows.Select(value => value.Value4).ToArray();
        _c5 = _rows.Select(value => value.Value5).ToArray();
        _c6 = _rows.Select(value => value.Value6).ToArray();
        _c7 = _rows.Select(value => value.Value7).ToArray();
        _c8 = _rows.Select(value => value.Value8).ToArray();
        _c9 = _rows.Select(value => value.Value9).ToArray();
        _c10 = _rows.Select(value => value.Value10).ToArray();
        _c11 = _rows.Select(value => value.Value11).ToArray();
        _c12 = _rows.Select(value => value.Value12).ToArray();
        _c13 = _rows.Select(value => value.Value13).ToArray();
        _c14 = _rows.Select(value => value.Value14).ToArray();
        _c15 = _rows.Select(value => value.Value15).ToArray();
        _c16 = _rows.Select(value => value.Value16).ToArray();
        _c17 = _rows.Select(value => value.Value17).ToArray();
        _c18 = _rows.Select(value => value.Value18).ToArray();
        _c19 = _rows.Select(value => value.Value19).ToArray();
        _c20 = _rows.Select(value => value.Value20).ToArray();
        _c21 = _rows.Select(value => value.Value21).ToArray();
        _rows = null!;
        _schema =
        [
            new ParquetSharp.Column<bool>("value_0"),
            new ParquetSharp.Column<bool>("value_1"),
            new ParquetSharp.Column<bool>("value_2"),
            new ParquetSharp.Column<bool>("value_3"),
            new ParquetSharp.Column<bool>("value_4"),
            new ParquetSharp.Column<bool>("value_5"),
            new ParquetSharp.Column<bool>("value_6"),
            new ParquetSharp.Column<bool>("value_7"),
            new ParquetSharp.Column<bool>("value_8"),
            new ParquetSharp.Column<bool>("value_9"),
            new ParquetSharp.Column<bool>("value_10"),
            new ParquetSharp.Column<bool>("value_11"),
            new ParquetSharp.Column<bool>("value_12"),
            new ParquetSharp.Column<bool>("value_13"),
            new ParquetSharp.Column<bool>("value_14"),
            new ParquetSharp.Column<bool>("value_15"),
            new ParquetSharp.Column<bool>("value_16"),
            new ParquetSharp.Column<bool>("value_17"),
            new ParquetSharp.Column<bool>("value_18"),
            new ParquetSharp.Column<bool>("value_19"),
            new ParquetSharp.Column<bool>("value_20"),
            new ParquetSharp.Column<bool>("value_21"),
        ];
        using var builder = new WriterPropertiesBuilder();
        builder
            .Compression(Compression.Uncompressed)
            .EnableStatistics()
            .EnableWritePageIndex()
            .DataPageVersion(ParquetSharp.ParquetDataPageVersion.V2);
        _properties = builder.DisableDictionary().Encoding(ParquetSharp.Encoding.Rle).Build();


        _outputBuffer = new byte[BenchmarkFixtures.GetOutputCapacity("SyntheticBooleanRleColumn", "ParquetSharp", out _expectedOutputBytes)];
    }
    [IterationSetup(Target = nameof(Write))]
    public void SetupWrite()
    {
        _output = BenchmarkFixtures.CreateOutput(_outputBuffer);
        _managedOutput = new ManagedOutputStream(_output, leaveOpen: true);
        _writer = new ParquetFileWriter(_managedOutput, _schema, _properties);
    }
    [Benchmark]
    public void Write()
    {
        for (var start = 0; start < Rows; start += RowsPerRowGroup)
        {
            var count = Math.Min(RowsPerRowGroup, Rows - start);
            using var group = _writer.AppendRowGroup();
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c0.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c1.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c2.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c3.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c4.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c5.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c6.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c7.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c8.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c9.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c10.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c11.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c12.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c13.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c14.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c15.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c16.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c17.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c18.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c19.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c20.AsSpan(start, count));
            using (var column = group.NextColumn().LogicalWriter<bool>())
                column.WriteBatch(_c21.AsSpan(start, count));
        }
        _writer.Close();
    }
    [IterationCleanup(Target = nameof(Write))]
    public void CleanupWrite()
    {
        BenchmarkFixtures.ValidateOutput(_expectedOutputBytes, BenchmarkFixtures.OutputLength(_output));
        _writer?.Dispose();
        _managedOutput?.Dispose();
        _output?.Dispose();
    }
    [GlobalSetup(Target = nameof(Read))]
    public void GlobalSetupRead()
    {

        var file = BenchmarkFixtures.LoadReadFile("SyntheticBooleanRle");
        _pinned = GCHandle.Alloc(file, GCHandleType.Pinned);
        _buffer = new NativeBuffer(_pinned.AddrOfPinnedObject(), file.LongLength);
        _source = new BufferReader(_buffer);

    }
    [IterationSetup(Target = nameof(Read))]
    public void SetupRead()
    {
        _reader?.Dispose();
        _reader = new ParquetFileReader(_source);
    }
    public ulong VerifyRead()
    {
        ulong sum = 0;
        long count = 0;
        for (var g = 0; g < _reader.FileMetaData.NumRowGroups; g++)
        {
            using var group = _reader.RowGroup(g);
            using (var column = group.Column(0).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read0);
                    sum = ColumnChecksum.Accumulate(_read0.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(1).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read1);
                    sum = ColumnChecksum.Accumulate(_read1.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(2).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read2);
                    sum = ColumnChecksum.Accumulate(_read2.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(3).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read3);
                    sum = ColumnChecksum.Accumulate(_read3.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(4).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read4);
                    sum = ColumnChecksum.Accumulate(_read4.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(5).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read5);
                    sum = ColumnChecksum.Accumulate(_read5.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(6).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read6);
                    sum = ColumnChecksum.Accumulate(_read6.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(7).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read7);
                    sum = ColumnChecksum.Accumulate(_read7.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(8).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read8);
                    sum = ColumnChecksum.Accumulate(_read8.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(9).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read9);
                    sum = ColumnChecksum.Accumulate(_read9.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(10).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read10);
                    sum = ColumnChecksum.Accumulate(_read10.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(11).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read11);
                    sum = ColumnChecksum.Accumulate(_read11.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(12).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read12);
                    sum = ColumnChecksum.Accumulate(_read12.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(13).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read13);
                    sum = ColumnChecksum.Accumulate(_read13.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(14).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read14);
                    sum = ColumnChecksum.Accumulate(_read14.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(15).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read15);
                    sum = ColumnChecksum.Accumulate(_read15.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(16).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read16);
                    sum = ColumnChecksum.Accumulate(_read16.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(17).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read17);
                    sum = ColumnChecksum.Accumulate(_read17.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(18).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read18);
                    sum = ColumnChecksum.Accumulate(_read18.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(19).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read19);
                    sum = ColumnChecksum.Accumulate(_read19.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(20).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read20);
                    sum = ColumnChecksum.Accumulate(_read20.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(21).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read21);
                    sum = ColumnChecksum.Accumulate(_read21.AsSpan(0, length), sum);
                    count += length;
                }
            }
        }
        if (count != (long)Rows * 22) throw new InvalidDataException($"Expected {(long)Rows * 22} values, got {count}.");
        return sum;
    }
    [Benchmark]
    public long Read()
    {
        long count = 0;
        for (var g = 0; g < _reader.FileMetaData.NumRowGroups; g++)
        {
            using var group = _reader.RowGroup(g);
            using (var column = group.Column(0).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read0);
                    ReadConsumption.Consume(_read0.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(1).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read1);
                    ReadConsumption.Consume(_read1.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(2).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read2);
                    ReadConsumption.Consume(_read2.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(3).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read3);
                    ReadConsumption.Consume(_read3.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(4).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read4);
                    ReadConsumption.Consume(_read4.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(5).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read5);
                    ReadConsumption.Consume(_read5.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(6).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read6);
                    ReadConsumption.Consume(_read6.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(7).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read7);
                    ReadConsumption.Consume(_read7.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(8).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read8);
                    ReadConsumption.Consume(_read8.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(9).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read9);
                    ReadConsumption.Consume(_read9.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(10).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read10);
                    ReadConsumption.Consume(_read10.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(11).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read11);
                    ReadConsumption.Consume(_read11.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(12).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read12);
                    ReadConsumption.Consume(_read12.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(13).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read13);
                    ReadConsumption.Consume(_read13.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(14).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read14);
                    ReadConsumption.Consume(_read14.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(15).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read15);
                    ReadConsumption.Consume(_read15.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(16).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read16);
                    ReadConsumption.Consume(_read16.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(17).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read17);
                    ReadConsumption.Consume(_read17.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(18).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read18);
                    ReadConsumption.Consume(_read18.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(19).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read19);
                    ReadConsumption.Consume(_read19.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(20).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read20);
                    ReadConsumption.Consume(_read20.AsSpan(0, length));
                    count += length;
                }
            }
            using (var column = group.Column(21).LogicalReader<bool>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read21);
                    ReadConsumption.Consume(_read21.AsSpan(0, length));
                    count += length;
                }
            }
        }
        if (count != (long)Rows * 22) throw new InvalidDataException($"Expected {(long)Rows * 22} values, got {count}.");
        return count;
    }
    [GlobalCleanup]
    public void Cleanup()
    {
        _reader?.Dispose();
        _source?.Dispose();
        _buffer?.Dispose();
        if (_pinned.IsAllocated) _pinned.Free();
        _properties?.Dispose();
        _output?.Dispose();
    }
}
