using System.Runtime.InteropServices;
using System.Text;
using System.Text.Json.Serialization;
using BenchmarkDotNet.Attributes;
using Parquet;
using Parquet.Serialization;
using ParquetSharp;
using ParquetSharp.IO;
using ParquetSharp.RowOriented;
using Plank.Reading;
using Plank.RowApi;
using Plank.Schema;
using Plank.Writing;
using NativeBuffer = ParquetSharp.IO.Buffer;
using Encoding = System.Text.Encoding;
using LogicalType = ParquetSharp.LogicalType;
using TimeUnit = ParquetSharp.TimeUnit;
using ParquetDataPageVersion = Plank.Writing.ParquetDataPageVersion;

namespace Plank.Benchmarks;

// Generated by scripts/generate_column_cases.py. Row cases remain unchanged.
// Generated by scripts/generate_column_cases.py. Multi adapters own one reader or serializer per column.
[MemoryDiagnoser]
public class RealTaxiPlainColumnMultiPlankBenchmarks
{
    int _columnWorkerCount;
    readonly ColumnParallelism.Tracker _parallelism = new();
    int _writeThreads;
    int _readThreads;
    MemoryReadSource[] _columnSources = null!;
    Plank.Reading.Logical.ParquetReader[] _columnReaders = null!;
    long[] _columnCounts = null!;

    const int RowsPerRowGroup = 1048576;
    RealTaxiPlainPlankRow[] _rows = null!;
    MemoryStream _output = null!;
    byte[] _outputBuffer = null!;
    long _expectedOutputBytes;
    int?[] _c0 = null!;
    DateTime?[] _c1 = null!;
    DateTime?[] _c2 = null!;
    long?[] _c3 = null!;
    double?[] _c4 = null!;
    long?[] _c5 = null!;
    ReadOnlyMemory<byte>?[] _c6 = null!;
    int?[] _c7 = null!;
    int?[] _c8 = null!;
    long?[] _c9 = null!;
    double?[] _c10 = null!;
    double?[] _c11 = null!;
    double?[] _c12 = null!;
    double?[] _c13 = null!;
    double?[] _c14 = null!;
    double?[] _c15 = null!;
    double?[] _c16 = null!;
    double?[] _c17 = null!;
    double?[] _c18 = null!;
    DefaultParquetBufferPool _pool = null!;
    Plank.Writing.ParquetWriter _writer = null!;
    MemoryReadSource _source = null!;
    Plank.Reading.Logical.ParquetReader _reader = null!;
    SerializedColumn<int?> _s0 = null!;
    SerializedColumn<DateTime?> _s1 = null!;
    SerializedColumn<DateTime?> _s2 = null!;
    SerializedColumn<long?> _s3 = null!;
    SerializedColumn<double?> _s4 = null!;
    SerializedColumn<long?> _s5 = null!;
    SerializedColumn<ReadOnlyMemory<byte>?> _s6 = null!;
    SerializedColumn<int?> _s7 = null!;
    SerializedColumn<int?> _s8 = null!;
    SerializedColumn<long?> _s9 = null!;
    SerializedColumn<double?> _s10 = null!;
    SerializedColumn<double?> _s11 = null!;
    SerializedColumn<double?> _s12 = null!;
    SerializedColumn<double?> _s13 = null!;
    SerializedColumn<double?> _s14 = null!;
    SerializedColumn<double?> _s15 = null!;
    SerializedColumn<double?> _s16 = null!;
    SerializedColumn<double?> _s17 = null!;
    SerializedColumn<double?> _s18 = null!;
    public IEnumerable<int> RowCounts => [BenchmarkData.TaxiRows];
    [ParamsSource(nameof(RowCounts))]
    public int Rows { get; set; }
    [GlobalSetup(Target = nameof(Write))]
    public void GlobalSetupWrite()
    {


        _rows ??= RealTaxiPlainPlankRow.FromSharp(BenchmarkData.LoadTaxiRows<RealTaxiPlainSharpRow>());
        foreach (var value in _rows)
        {
            value.Pickup = value.Pickup.HasValue ? DateTime.SpecifyKind(value.Pickup.GetValueOrDefault(), DateTimeKind.Utc) : null;
            value.Dropoff = value.Dropoff.HasValue ? DateTime.SpecifyKind(value.Dropoff.GetValueOrDefault(), DateTimeKind.Utc) : null;
        }
        _c0 = _rows.Select(value => value.VendorId).ToArray();
        _c1 = _rows.Select(value => value.Pickup).ToArray();
        _c2 = _rows.Select(value => value.Dropoff).ToArray();
        _c3 = _rows.Select(value => value.PassengerCount).ToArray();
        _c4 = _rows.Select(value => value.TripDistance).ToArray();
        _c5 = _rows.Select(value => value.RatecodeId).ToArray();
        _c6 = _rows.Select(value => value.StoreAndForwardFlag).ToArray();
        _c7 = _rows.Select(value => value.PickupLocationId).ToArray();
        _c8 = _rows.Select(value => value.DropoffLocationId).ToArray();
        _c9 = _rows.Select(value => value.PaymentType).ToArray();
        _c10 = _rows.Select(value => value.FareAmount).ToArray();
        _c11 = _rows.Select(value => value.Extra).ToArray();
        _c12 = _rows.Select(value => value.MtaTax).ToArray();
        _c13 = _rows.Select(value => value.TipAmount).ToArray();
        _c14 = _rows.Select(value => value.TollsAmount).ToArray();
        _c15 = _rows.Select(value => value.ImprovementSurcharge).ToArray();
        _c16 = _rows.Select(value => value.TotalAmount).ToArray();
        _c17 = _rows.Select(value => value.CongestionSurcharge).ToArray();
        _c18 = _rows.Select(value => value.AirportFee).ToArray();
        _rows = null!;
        _pool = new DefaultParquetBufferPool(ParquetBufferRetentionPolicy.ZeroAllocation);
        using var preparationOutput = new MemoryStream();
        _writer = RealTaxiPlainPlankRow.Schema.CreateWriter(preparationOutput, new ParquetWriterOptions
        { Compression = CompressionKind.None, BufferPool = _pool, DataPageVersion = ParquetDataPageVersion.V2,
           WritePageIndexes = true, WritePageCrc = false });
        _s0 = _writer.CreateSerializedColumn<int?>(RealTaxiPlainPlankRow.Schema.LeafColumns[0]);
        _s1 = _writer.CreateSerializedColumn<DateTime?>(RealTaxiPlainPlankRow.Schema.LeafColumns[1]);
        _s2 = _writer.CreateSerializedColumn<DateTime?>(RealTaxiPlainPlankRow.Schema.LeafColumns[2]);
        _s3 = _writer.CreateSerializedColumn<long?>(RealTaxiPlainPlankRow.Schema.LeafColumns[3]);
        _s4 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[4]);
        _s5 = _writer.CreateSerializedColumn<long?>(RealTaxiPlainPlankRow.Schema.LeafColumns[5]);
        _s6 = _writer.CreateSerializedColumn<ReadOnlyMemory<byte>?>(RealTaxiPlainPlankRow.Schema.LeafColumns[6]);
        _s7 = _writer.CreateSerializedColumn<int?>(RealTaxiPlainPlankRow.Schema.LeafColumns[7]);
        _s8 = _writer.CreateSerializedColumn<int?>(RealTaxiPlainPlankRow.Schema.LeafColumns[8]);
        _s9 = _writer.CreateSerializedColumn<long?>(RealTaxiPlainPlankRow.Schema.LeafColumns[9]);
        _s10 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[10]);
        _s11 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[11]);
        _s12 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[12]);
        _s13 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[13]);
        _s14 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[14]);
        _s15 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[15]);
        _s16 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[16]);
        _s17 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[17]);
        _s18 = _writer.CreateSerializedColumn<double?>(RealTaxiPlainPlankRow.Schema.LeafColumns[18]);
        _outputBuffer = new byte[BenchmarkFixtures.GetOutputCapacity("RealTaxiPlainColumn", "Plank", out _expectedOutputBytes)];
        _columnWorkerCount = ColumnParallelism.WorkerCount(19);
    }
    [IterationSetup(Target = nameof(Write))]
    public void SetupWrite()
    {
        _output = BenchmarkFixtures.CreateOutput(_outputBuffer);
        _writer.Reset(_output);
    }
    void SerializeColumn(int ordinal, int start, int count)
    {
        switch (ordinal)
        {
            case 0: _s0.Serialize(_c0.AsSpan(start, count)); break;
            case 1: _s1.Serialize(_c1.AsSpan(start, count)); break;
            case 2: _s2.Serialize(_c2.AsSpan(start, count)); break;
            case 3: _s3.Serialize(_c3.AsSpan(start, count)); break;
            case 4: _s4.Serialize(_c4.AsSpan(start, count)); break;
            case 5: _s5.Serialize(_c5.AsSpan(start, count)); break;
            case 6: _s6.Serialize(_c6.AsSpan(start, count)); break;
            case 7: _s7.Serialize(_c7.AsSpan(start, count)); break;
            case 8: _s8.Serialize(_c8.AsSpan(start, count)); break;
            case 9: _s9.Serialize(_c9.AsSpan(start, count)); break;
            case 10: _s10.Serialize(_c10.AsSpan(start, count)); break;
            case 11: _s11.Serialize(_c11.AsSpan(start, count)); break;
            case 12: _s12.Serialize(_c12.AsSpan(start, count)); break;
            case 13: _s13.Serialize(_c13.AsSpan(start, count)); break;
            case 14: _s14.Serialize(_c14.AsSpan(start, count)); break;
            case 15: _s15.Serialize(_c15.AsSpan(start, count)); break;
            case 16: _s16.Serialize(_c16.AsSpan(start, count)); break;
            case 17: _s17.Serialize(_c17.AsSpan(start, count)); break;
            case 18: _s18.Serialize(_c18.AsSpan(start, count)); break;
            default: throw new ArgumentOutOfRangeException(nameof(ordinal));
        }
    }
    [Benchmark]
    public void Write()
    {
        for (var start = 0; start < Rows; start += RowsPerRowGroup)
        {
            var count = Math.Min(RowsPerRowGroup, Rows - start);
            _writeThreads = Math.Max(_writeThreads, _parallelism.Run(19, _columnWorkerCount,
                index => SerializeColumn(index, start, count)));
            var group = _writer.StartRowGroup();
            group.Write(_s0);
            group.Write(_s1);
            group.Write(_s2);
            group.Write(_s3);
            group.Write(_s4);
            group.Write(_s5);
            group.Write(_s6);
            group.Write(_s7);
            group.Write(_s8);
            group.Write(_s9);
            group.Write(_s10);
            group.Write(_s11);
            group.Write(_s12);
            group.Write(_s13);
            group.Write(_s14);
            group.Write(_s15);
            group.Write(_s16);
            group.Write(_s17);
            group.Write(_s18);
        }
        _writer.CloseFile();
    }
    [IterationCleanup(Target = nameof(Write))]
    public void CleanupWrite()
    {
        BenchmarkFixtures.ValidateOutput(_expectedOutputBytes, BenchmarkFixtures.OutputLength(_output));
        ColumnParallelism.WriteMarker(GetType().Name, "write", _writeThreads);
        _output?.Dispose();
    }
    [GlobalSetup(Target = nameof(Read))]
    public void GlobalSetupRead()
    {
        _pool = new DefaultParquetBufferPool(ParquetBufferRetentionPolicy.ZeroAllocation);
        var file = BenchmarkFixtures.LoadReadFile("RealTaxiPlain");
        _columnSources = new MemoryReadSource[19];
        _columnReaders = new Plank.Reading.Logical.ParquetReader[19];
        _columnCounts = new long[19];
        for (var index = 0; index < 19; index++)
        {
            _columnSources[index] = new MemoryReadSource(file);
            _columnReaders[index] = new Plank.Reading.Logical.ParquetReader(
                new Plank.Reading.Logical.ParquetReaderOptions { BufferPool = _pool });
        }
        _source = _columnSources[0];
        _reader = _columnReaders[0];
        _columnWorkerCount = ColumnParallelism.WorkerCount(19);
    }
    [IterationSetup(Target = nameof(Read))]
    public void SetupRead()
    {
        for (var index = 0; index < 19; index++)
            _columnReaders[index].Reset(_columnSources[index]);
    }
    public double VerifyRead()
    {
        double sum = 0;
        long count = 0;
        foreach (var group in _reader.RowGroups)
        {
            foreach (var buffer in group.Column<int?>(0))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<DateTime?>(1))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<DateTime?>(2))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<long?>(3))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(4))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<long?>(5))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<byte>(6))
            {
                for (var index = 0; index < buffer.Count; index++)
                    sum += (double)(buffer.IsNull(index) ? 0 : buffer.GetValue(index).Length);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<int?>(7))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<int?>(8))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<long?>(9))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(10))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(11))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(12))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(13))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(14))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(15))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(16))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(17))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
            foreach (var buffer in group.Column<double?>(18))
            {
                sum = ColumnChecksum.Accumulate(buffer.Values, sum);
                count += buffer.Count;
            }
        }
        if (count != (long)Rows * 19) throw new InvalidDataException($"Expected {(long)Rows * 19} values, got {count}.");
        return sum;
    }

    long ReadColumn0()
    {
        long count = 0;
        foreach (var group in _columnReaders[0].RowGroups)
        {
            foreach (var buffer in group.Column<int?>(0))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn1()
    {
        long count = 0;
        foreach (var group in _columnReaders[1].RowGroups)
        {
            foreach (var buffer in group.Column<DateTime?>(1))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn2()
    {
        long count = 0;
        foreach (var group in _columnReaders[2].RowGroups)
        {
            foreach (var buffer in group.Column<DateTime?>(2))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn3()
    {
        long count = 0;
        foreach (var group in _columnReaders[3].RowGroups)
        {
            foreach (var buffer in group.Column<long?>(3))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn4()
    {
        long count = 0;
        foreach (var group in _columnReaders[4].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(4))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn5()
    {
        long count = 0;
        foreach (var group in _columnReaders[5].RowGroups)
        {
            foreach (var buffer in group.Column<long?>(5))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn6()
    {
        long count = 0;
        foreach (var group in _columnReaders[6].RowGroups)
        {
            foreach (var buffer in group.Column<byte>(6))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn7()
    {
        long count = 0;
        foreach (var group in _columnReaders[7].RowGroups)
        {
            foreach (var buffer in group.Column<int?>(7))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn8()
    {
        long count = 0;
        foreach (var group in _columnReaders[8].RowGroups)
        {
            foreach (var buffer in group.Column<int?>(8))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn9()
    {
        long count = 0;
        foreach (var group in _columnReaders[9].RowGroups)
        {
            foreach (var buffer in group.Column<long?>(9))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn10()
    {
        long count = 0;
        foreach (var group in _columnReaders[10].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(10))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn11()
    {
        long count = 0;
        foreach (var group in _columnReaders[11].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(11))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn12()
    {
        long count = 0;
        foreach (var group in _columnReaders[12].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(12))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn13()
    {
        long count = 0;
        foreach (var group in _columnReaders[13].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(13))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn14()
    {
        long count = 0;
        foreach (var group in _columnReaders[14].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(14))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn15()
    {
        long count = 0;
        foreach (var group in _columnReaders[15].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(15))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn16()
    {
        long count = 0;
        foreach (var group in _columnReaders[16].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(16))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn17()
    {
        long count = 0;
        foreach (var group in _columnReaders[17].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(17))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn18()
    {
        long count = 0;
        foreach (var group in _columnReaders[18].RowGroups)
        {
            foreach (var buffer in group.Column<double?>(18))
            {
                ReadConsumption.Consume(buffer.Values);
                count += buffer.Count;
            }
        }
        return count;
    }

    long ReadColumn(int ordinal)
        => ordinal switch
        {
            0 => ReadColumn0(),
            1 => ReadColumn1(),
            2 => ReadColumn2(),
            3 => ReadColumn3(),
            4 => ReadColumn4(),
            5 => ReadColumn5(),
            6 => ReadColumn6(),
            7 => ReadColumn7(),
            8 => ReadColumn8(),
            9 => ReadColumn9(),
            10 => ReadColumn10(),
            11 => ReadColumn11(),
            12 => ReadColumn12(),
            13 => ReadColumn13(),
            14 => ReadColumn14(),
            15 => ReadColumn15(),
            16 => ReadColumn16(),
            17 => ReadColumn17(),
            18 => ReadColumn18(),
            _ => throw new ArgumentOutOfRangeException(nameof(ordinal))
        };

    [Benchmark]
    public long Read()
    {
        _parallelism.Run(19, _columnWorkerCount,
            index => _columnCounts[index] = ReadColumn(index));
        long count = 0;
        for (var index = 0; index < 19; index++)
            count += _columnCounts[index];
        if (count != (long)Rows * 19)
            throw new InvalidDataException($"Expected {(long)Rows * 19} values, got {count}.");
        _readThreads = _parallelism.LastObserved;
        return count;
    }

    [GlobalCleanup]
    public void Cleanup()
    {
        if (_readThreads > 0)
            ColumnParallelism.WriteMarker(GetType().Name, "read", _readThreads);
        foreach (var reader in _columnReaders ?? [])
            reader?.Dispose();
        foreach (var source in _columnSources ?? [])
            source?.Dispose();
        _writer?.Dispose();
        _pool?.Dispose();
        _output?.Dispose();
    }
}
[MemoryDiagnoser]
public class RealTaxiPlainColumnMultiParquetSharpBenchmarks
{
    int _columnWorkerCount;
    readonly ColumnParallelism.Tracker _parallelism = new();
    int _readThreads;
    ParquetFileReader[] _columnReaders = null!;
    BufferReader[] _columnSources = null!;
    long[] _columnCounts = null!;

    const int RowsPerRowGroup = 1048576;
    ParquetFileReader _reader = null!;
    GCHandle _pinned;
    NativeBuffer _buffer = null!;
    BufferReader _source = null!;
    int?[] _read0 = new int?[65536];
    DateTime?[] _read1 = new DateTime?[65536];
    DateTime?[] _read2 = new DateTime?[65536];
    long?[] _read3 = new long?[65536];
    double?[] _read4 = new double?[65536];
    long?[] _read5 = new long?[65536];
    string?[] _read6 = new string?[65536];
    int?[] _read7 = new int?[65536];
    int?[] _read8 = new int?[65536];
    long?[] _read9 = new long?[65536];
    double?[] _read10 = new double?[65536];
    double?[] _read11 = new double?[65536];
    double?[] _read12 = new double?[65536];
    double?[] _read13 = new double?[65536];
    double?[] _read14 = new double?[65536];
    double?[] _read15 = new double?[65536];
    double?[] _read16 = new double?[65536];
    double?[] _read17 = new double?[65536];
    double?[] _read18 = new double?[65536];
    public IEnumerable<int> RowCounts => [BenchmarkData.TaxiRows];
    [ParamsSource(nameof(RowCounts))]
    public int Rows { get; set; }




    [GlobalSetup(Target = nameof(Read))]
    public void GlobalSetupRead()
    {
        var file = BenchmarkFixtures.LoadReadFile("RealTaxiPlain");
        _pinned = GCHandle.Alloc(file, GCHandleType.Pinned);
        _buffer = new NativeBuffer(_pinned.AddrOfPinnedObject(), file.LongLength);
        _columnSources = new BufferReader[19];
        for (var index = 0; index < 19; index++)
            _columnSources[index] = new BufferReader(_buffer);
        _source = _columnSources[0];
        _columnReaders = new ParquetFileReader[19];
        _columnCounts = new long[19];
        _columnWorkerCount = ColumnParallelism.WorkerCount(19);
    }
    [IterationSetup(Target = nameof(Read))]
    public void SetupRead()
    {
        foreach (var reader in _columnReaders ?? [])
            reader?.Dispose();
        _columnReaders = new ParquetFileReader[19];
        for (var index = 0; index < 19; index++)
            _columnReaders[index] = new ParquetFileReader(_columnSources[index]);
        _reader = _columnReaders[0];
    }
    public double VerifyRead()
    {
        double sum = 0;
        long count = 0;
        for (var g = 0; g < _reader.FileMetaData.NumRowGroups; g++)
        {
            using var group = _reader.RowGroup(g);
            using (var column = group.Column(0).LogicalReader<int?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read0);
                    sum = ColumnChecksum.Accumulate(_read0.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(1).LogicalReader<DateTime?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read1);
                    sum = ColumnChecksum.Accumulate(_read1.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(2).LogicalReader<DateTime?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read2);
                    sum = ColumnChecksum.Accumulate(_read2.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(3).LogicalReader<long?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read3);
                    sum = ColumnChecksum.Accumulate(_read3.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(4).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read4);
                    sum = ColumnChecksum.Accumulate(_read4.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(5).LogicalReader<long?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read5);
                    sum = ColumnChecksum.Accumulate(_read5.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(6).LogicalReader<string?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read6);
                    for (var index = 0; index < length; index++)
                    {
                        var value = _read6[index];
                        sum += (ulong)(value?.Length ?? 0);
                    }
                    count += length;
                }
            }
            using (var column = group.Column(7).LogicalReader<int?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read7);
                    sum = ColumnChecksum.Accumulate(_read7.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(8).LogicalReader<int?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read8);
                    sum = ColumnChecksum.Accumulate(_read8.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(9).LogicalReader<long?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read9);
                    sum = ColumnChecksum.Accumulate(_read9.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(10).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read10);
                    sum = ColumnChecksum.Accumulate(_read10.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(11).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read11);
                    sum = ColumnChecksum.Accumulate(_read11.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(12).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read12);
                    sum = ColumnChecksum.Accumulate(_read12.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(13).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read13);
                    sum = ColumnChecksum.Accumulate(_read13.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(14).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read14);
                    sum = ColumnChecksum.Accumulate(_read14.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(15).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read15);
                    sum = ColumnChecksum.Accumulate(_read15.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(16).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read16);
                    sum = ColumnChecksum.Accumulate(_read16.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(17).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read17);
                    sum = ColumnChecksum.Accumulate(_read17.AsSpan(0, length), sum);
                    count += length;
                }
            }
            using (var column = group.Column(18).LogicalReader<double?>())
            {
                while (column.HasNext)
                {
                    var length = column.ReadBatch(_read18);
                    sum = ColumnChecksum.Accumulate(_read18.AsSpan(0, length), sum);
                    count += length;
                }
            }
        }
        if (count != (long)Rows * 19) throw new InvalidDataException($"Expected {(long)Rows * 19} values, got {count}.");
        return sum;
    }

    long ReadColumn0()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[0].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[0].RowGroup(g);
            using var column = group.Column(0).LogicalReader<int?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read0);
                ReadConsumption.Consume(_read0.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn1()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[1].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[1].RowGroup(g);
            using var column = group.Column(1).LogicalReader<DateTime?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read1);
                ReadConsumption.Consume(_read1.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn2()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[2].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[2].RowGroup(g);
            using var column = group.Column(2).LogicalReader<DateTime?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read2);
                ReadConsumption.Consume(_read2.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn3()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[3].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[3].RowGroup(g);
            using var column = group.Column(3).LogicalReader<long?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read3);
                ReadConsumption.Consume(_read3.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn4()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[4].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[4].RowGroup(g);
            using var column = group.Column(4).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read4);
                ReadConsumption.Consume(_read4.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn5()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[5].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[5].RowGroup(g);
            using var column = group.Column(5).LogicalReader<long?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read5);
                ReadConsumption.Consume(_read5.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn6()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[6].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[6].RowGroup(g);
            using var column = group.Column(6).LogicalReader<string?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read6);
                ReadConsumption.Consume(_read6.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn7()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[7].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[7].RowGroup(g);
            using var column = group.Column(7).LogicalReader<int?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read7);
                ReadConsumption.Consume(_read7.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn8()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[8].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[8].RowGroup(g);
            using var column = group.Column(8).LogicalReader<int?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read8);
                ReadConsumption.Consume(_read8.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn9()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[9].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[9].RowGroup(g);
            using var column = group.Column(9).LogicalReader<long?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read9);
                ReadConsumption.Consume(_read9.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn10()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[10].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[10].RowGroup(g);
            using var column = group.Column(10).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read10);
                ReadConsumption.Consume(_read10.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn11()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[11].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[11].RowGroup(g);
            using var column = group.Column(11).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read11);
                ReadConsumption.Consume(_read11.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn12()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[12].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[12].RowGroup(g);
            using var column = group.Column(12).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read12);
                ReadConsumption.Consume(_read12.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn13()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[13].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[13].RowGroup(g);
            using var column = group.Column(13).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read13);
                ReadConsumption.Consume(_read13.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn14()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[14].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[14].RowGroup(g);
            using var column = group.Column(14).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read14);
                ReadConsumption.Consume(_read14.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn15()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[15].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[15].RowGroup(g);
            using var column = group.Column(15).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read15);
                ReadConsumption.Consume(_read15.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn16()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[16].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[16].RowGroup(g);
            using var column = group.Column(16).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read16);
                ReadConsumption.Consume(_read16.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn17()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[17].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[17].RowGroup(g);
            using var column = group.Column(17).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read17);
                ReadConsumption.Consume(_read17.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn18()
    {
        long count = 0;
        for (var g = 0; g < _columnReaders[18].FileMetaData.NumRowGroups; g++)
        {
            using var group = _columnReaders[18].RowGroup(g);
            using var column = group.Column(18).LogicalReader<double?>();
            while (column.HasNext)
            {
                var length = column.ReadBatch(_read18);
                ReadConsumption.Consume(_read18.AsSpan(0, length));
                count += length;
            }
        }
        return count;
    }

    long ReadColumn(int ordinal)
        => ordinal switch
        {
            0 => ReadColumn0(),
            1 => ReadColumn1(),
            2 => ReadColumn2(),
            3 => ReadColumn3(),
            4 => ReadColumn4(),
            5 => ReadColumn5(),
            6 => ReadColumn6(),
            7 => ReadColumn7(),
            8 => ReadColumn8(),
            9 => ReadColumn9(),
            10 => ReadColumn10(),
            11 => ReadColumn11(),
            12 => ReadColumn12(),
            13 => ReadColumn13(),
            14 => ReadColumn14(),
            15 => ReadColumn15(),
            16 => ReadColumn16(),
            17 => ReadColumn17(),
            18 => ReadColumn18(),
            _ => throw new ArgumentOutOfRangeException(nameof(ordinal))
        };

    [Benchmark]
    public long Read()
    {
        _parallelism.Run(19, _columnWorkerCount,
            index => _columnCounts[index] = ReadColumn(index));
        long count = 0;
        for (var index = 0; index < 19; index++)
            count += _columnCounts[index];
        if (count != (long)Rows * 19)
            throw new InvalidDataException($"Expected {(long)Rows * 19} values, got {count}.");
        _readThreads = _parallelism.LastObserved;
        return count;
    }

    [GlobalCleanup]
    public void Cleanup()
    {
        if (_readThreads > 0)
            ColumnParallelism.WriteMarker(GetType().Name, "read", _readThreads);
        foreach (var reader in _columnReaders ?? [])
            reader?.Dispose();
        foreach (var source in _columnSources ?? [])
            source?.Dispose();
        _buffer?.Dispose();
        if (_pinned.IsAllocated) _pinned.Free();
    }
}
