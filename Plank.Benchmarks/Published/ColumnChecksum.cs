using System.Runtime.CompilerServices;

namespace Plank.Benchmarks;

// Generated by scripts/generate_column_cases.py.
static class ColumnChecksum
{
    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static long Accumulate(ReadOnlySpan<DateTime> values, long sum)
    {
        foreach (var value in values) sum += value.Ticks;
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static double Accumulate(ReadOnlySpan<DateTime?> values, double sum)
    {
        foreach (var value in values) sum += (value?.Ticks ?? 0L);
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static long Accumulate(ReadOnlySpan<DateTime?> values, long sum)
    {
        foreach (var value in values) sum += (value?.Ticks ?? 0L);
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static ulong Accumulate(ReadOnlySpan<bool> values, ulong sum)
    {
        foreach (var value in values) sum += (value ? 1UL : 0UL);
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static double Accumulate(ReadOnlySpan<double> values, double sum)
    {
        foreach (var value in values) sum += value;
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static double Accumulate(ReadOnlySpan<double?> values, double sum)
    {
        foreach (var value in values) sum += value.GetValueOrDefault();
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static ulong Accumulate(ReadOnlySpan<int> values, ulong sum)
    {
        foreach (var value in values) sum += unchecked((uint)value);
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static double Accumulate(ReadOnlySpan<int?> values, double sum)
    {
        foreach (var value in values) sum += unchecked((uint)value.GetValueOrDefault());
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static ulong Accumulate(ReadOnlySpan<int?> values, ulong sum)
    {
        foreach (var value in values) sum += unchecked((uint)value.GetValueOrDefault());
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static ulong Accumulate(ReadOnlySpan<long> values, ulong sum)
    {
        foreach (var value in values) sum += unchecked((ulong)value);
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static double Accumulate(ReadOnlySpan<long?> values, double sum)
    {
        foreach (var value in values) sum += unchecked((ulong)value.GetValueOrDefault());
        return sum;
    }

    [MethodImpl(MethodImplOptions.NoInlining)]
    internal static ulong Accumulate(ReadOnlySpan<long?> values, ulong sum)
    {
        foreach (var value in values) sum += unchecked((ulong)value.GetValueOrDefault());
        return sum;
    }

}
